version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: quicksync-backend
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=my_secure_secret_123
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_WS_URL: ws://localhost:8000
    container_name: quicksync-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped
